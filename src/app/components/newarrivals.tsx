import React from "react";
import Carousel from "../(components)/Carousel";

const Newarrivals = () => {
  return (
    <div>
      <Carousel name="New Arrivals" />
    </div>
  );
};

export default Newarrivals;
